# Copy this file to .env (in this same folder) and fill in real keys.
# The .env file is gitignored and is picked up automatically by
# `firebase deploy --only functions`.
#
# Leave a key empty to keep that provider in the "coming soon" state.

GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
